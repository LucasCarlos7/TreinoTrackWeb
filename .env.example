# Copie para .env.local no desenvolvimento local (não commitar o .env.local).
# No Render, esta variável é definida na env var VITE_API_URL do Static Site
# e só tem efeito no momento do BUILD (não em runtime).
VITE_API_URL=http://localhost:8080/api
